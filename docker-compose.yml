services:
  mysql-db1:
    image: mysql:latest
    container_name: mysql-db1
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin1
      MYSQL_DATABASE: testdb
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: